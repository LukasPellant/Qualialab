
import { Typography, Container } from '@mui/material';

const EquipmentPage = () => {
  return (
    <Container>
      <Typography variant="h2" gutterBottom>Vybavení</Typography>
      <Typography variant="body1">
        Zde bude seznam a popis FPV vybavení, které používám.
      </Typography>
    </Container>
  );
};

export default EquipmentPage;
